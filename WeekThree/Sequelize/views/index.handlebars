<h1>Restaurants</h1>
<section>
    {{#each restaurants}}

    <article>
        <header>{{this.name}}</header>
        <img src="{{this.image}}" alt="stuff" width="500" height="600">

        <main>
            {{#each this.menus}}        
                <p>{{this.title}}</p>
                {{#each this.items}}
                    <p>{{this.name}} - {{this.price}}</p>
                {{/each}}
            {{/each}}
        </main>

    </article>
    {{/each}}
</section>
